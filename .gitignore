# Compiled binaries
cmd/*/bonfyre-*
!cmd/*/src/
!cmd/*/bonfyre-*-selftest
cmd/*/build/
cmd/*/obj/
lib/liblambda-tensors/*.a
lib/liblambda-tensors/*.so
lib/liblambda-tensors/*.o
lib/liblambda-tensors/src/*.o
lib/liblambda-tensors/test_lambda_tensors
lib/*/obj/

# Python virtual environments
venv/
__pycache__/
*.pyc

# Build artifacts
*.o
*.a
*.so
*.dylib
*.dSYM/
/zig-out/
/.zig-cache/

# SQLite databases (runtime state)
*.db
*.db-journal
*.db-wal
*.db-shm
*.duckdb

# OS files
.DS_Store
Thumbs.db
*.swp
*~

# IDE
.vscode/
.idea/
*.code-workspace

# Runtime data
uploads/
--out/
wire_raw/
raw-captures/
*.pcap
*.pcapng

# Node
node_modules/
dist/
.npmrc

# Test outputs
outputs-*/

# Koopman mode checkpoints (large tensors — regenerate with learn_modes.py)
**/*.pt
**/*.koopman

# Secrets / env files — never commit
.env
**/.env
pipeline/.env
**/pipeline/.env

# Model files (large binaries — never commit)
*.gguf
*.fpq
*.safetensors
*.bin
*.pth
*.pt
*.onnx
*.tflite
*.pb
*.h5
*.hdf5
*.mlmodel
*.mlpackage
*.ckpt
*.engine
*.plan
*.mar
*.torchscript

# Model directories
models/
*.cache/huggingface/

# Generated Frappe bench (framework + venv, not source) — never track
integrations/frappe-bench/

# BonfyreFrappeCompiler build output (regenerated by scripts, not source)
/output/
