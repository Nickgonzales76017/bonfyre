Source material for the real `wiki_page` entry (id 2, namespace `aurekai`) in
Bonfyre's native CMS — see [`content/README.md`](../../README.md).

`notes/chain/` is the output of `akai publish chain --surface agentguard-draft`
run on `notes/case-study-source.md`, staged locally at
`~/.aurekai/surfaces/agentguard-draft/` — not external distribution.

Status: draft. Underlying story is unresolved — PRs #213/#216/#217/#218 are
still open, sequential review in progress. Don't write this up as a finished
case study until at least one merges.
