Source material for the real `wiki_page` entry (id 3, namespace `aurekai`) in
Bonfyre's native CMS — see [`content/README.md`](../../README.md).

`notes/chain/` is the output of `akai publish chain --surface hqq-draft` run
on `notes/case-study-source.md`, staged locally at
`~/.aurekai/surfaces/hqq-draft/` — not external distribution.

Status: draft, but the underlying story is resolved — the correction is
already published and merged (bonfyre-fpq-benchmark PR #1). Draft here
means "not yet written up as an article," not "not yet true."
