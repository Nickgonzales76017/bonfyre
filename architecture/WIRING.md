# Wiring map (generated)

Generated by `python3 architecture/atlas.py wiring-doc` from the atlas. Do
not hand-edit. It shows how the estate wires into itself -- feedback loops
(an organ whose effect returns as evidence) versus one-way flows.

## Interaction graph

- 27 interacting architectures, 36 interactions
- feedback loops closed: 2; one-way flows: 25

## WiringSpec fact flow

- 33 organs carry a WiringSpec, over 45 shared facts
- duplicate ownership: 0 (validate fails if > 0)

### Closed fact feedback loops

- **21 organs**: address-plane ↔ aurekai ↔ bonfyre-fs ↔ capability-closure ↔ drive ↔ erpnext ↔ feldera ↔ helpdesk ↔ hrms ↔ insights ↔ kv-temporal ↔ prepared-execution-graph ↔ proof-frontier ↔ provider-state ↔ resource-activation ↔ resource-admission ↔ surface-ir ↔ transport-phenotype ↔ unlock-graph ↔ wiki ↔ work-graph
- **5 organs**: actor-graph ↔ collapse-front ↔ crm ↔ fortification-plan ↔ verification-ledger
- **2 organs**: authority-graph ↔ frappe-core

### Open gaps (the next wiring to build)

Orphan consumers — an organ expects a fact nobody provides:
- `ExternalEvent` ← occurrence-spine

Orphan producers — published, nobody consumes (terminal surface or unwired):
- `ResourceGrant` → resource-admission
- `SurfaceDelta` → surface-ir
- `SurfaceIR` → surface-ir

